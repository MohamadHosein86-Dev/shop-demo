
export interface ChildrenType{
    children:React.ReactNode
}
export interface productsType {
  id: string;
  title: string;
  price: number;
  stock: boolean;
  rating: number;
  image: string;
  description: string;
}