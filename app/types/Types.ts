
export interface ChildrenType{
    children:React.ReactNode
}
export interface productsType {
  id: string;
  name: string;
  price: number;
  stock: boolean;
  rating: number;
  image: string;
  description: string;
}